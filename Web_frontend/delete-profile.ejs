<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Delete Profile</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            font-family: 'Poppins', sans-serif;
            min-height: 100vh;
            margin: 0;
        }
        .delete-container {
            max-width: 400px;
            margin: 80px auto;
            background: white;
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(44,62,80,0.15);
            padding: 2.5rem 2rem 2rem 2rem;
            text-align: center;
            position: relative;
        }
        .delete-logo {
            font-size: 2rem;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 1.2rem;
        }
        .delete-logo span {
            color: #3498db;
        }
        h1 {
            font-size: 1.5rem;
            color: #e74c3c;
            margin-bottom: 1.2rem;
            font-weight: 600;
        }
        .delete-message {
            font-size: 1.1rem;
            color: #2c3e50;
            margin-bottom: 2rem;
        }
        .delete-btn, .cancel-btn {
            min-width: 120px;
            background: linear-gradient(90deg, #e74c3c 0%, #f7971e 100%);
            color: #fff;
            border: none;
            border-radius: 8px;
            padding: 0.7rem 0.5rem;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s;
            box-shadow: 0 2px 8px rgba(44,62,80,0.08);
            margin: 0 0.7rem 1.2rem 0.7rem;
            display: inline-block;
        }
        .delete-btn:hover {
            background: linear-gradient(90deg, #c0392b 0%, #f7971e 100%);
        }
        .cancel-btn {
            background: linear-gradient(90deg, #3498db 0%, #43e97b 100%);
        }
        .cancel-btn:hover {
            background: linear-gradient(90deg, #2980b9 0%, #43e97b 100%);
        }
        .back-link {
            display: block;
            text-align: center;
            color: #3498db;
            text-decoration: none;
            font-weight: 500;
            font-size: 1.05rem;
            transition: color 0.2s;
        }
        .back-link:hover {
            text-decoration: underline;
            color: #2980b9;
        }
    </style>
</head>
<body>
    <div class="delete-container">
        <div class="delete-logo">Sign<span>Ease</span></div>
        <h1>Delete Profile</h1>
        <div class="delete-message">
            Are you sure you want to delete your profile? This action cannot be undone.
        </div>
        <form action="/delete-profile" method="POST" style="display:inline;">
            <button type="submit" class="delete-btn">Yes, Delete</button>
        </form>
        <a href="/profile" class="cancel-btn">Cancel</a>
        <a href="/profile" class="back-link">Back to Profile</a>
    </div>
</body>
</html>

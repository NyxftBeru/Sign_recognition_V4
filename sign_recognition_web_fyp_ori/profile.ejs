<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Profile</title>
    <link rel="stylesheet" href="test.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            font-family: 'Poppins', sans-serif;
            min-height: 100vh;
            margin: 0;
        }
        .profile-container {
            max-width: 400px;
            margin: 80px auto;
            background: white;
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(44,62,80,0.15);
            padding: 2.5rem 2rem 2rem 2rem;
            text-align: center;
            position: relative;
        }
        .profile-logo {
            font-size: 2rem;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 1.2rem;
        }
        .profile-logo span {
            color: #3498db;
        }
        h1 {
            font-size: 1.7rem;
            color: #3498db;
            margin-bottom: 1.2rem;
            font-weight: 600;
        }
        .profile-info {
            font-size: 1.15rem;
            color: #2c3e50;
            margin-bottom: 2rem;
        }
        .logout-btn {
            min-width: 120px;
            background: linear-gradient(90deg, #3498db 0%, #43e97b 100%);
            color: #fff;
            border: none;
            border-radius: 8px;
            padding: 0.7rem 0.5rem;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s;
            box-shadow: 0 2px 8px rgba(44,62,80,0.08);
            margin-bottom: 1.2rem;
            margin-right: 0.7rem;
            display: inline-block;
        }
        .logout-btn:hover {
            background: linear-gradient(90deg, #2980b9 0%, #43e97b 100%);
        }
        .back-link {
            display: block;
            text-align: center;
            color: #3498db;
            text-decoration: none;
            font-weight: 500;
            font-size: 1.05rem;
            transition: color 0.2s;
        }
        .back-link:hover {
            text-decoration: underline;
            color: #2980b9;
        }
    </style>
</head>
<body>
    <div class="profile-container">
        <div class="profile-logo">Sign<span>Ease</span></div>
        <h1>Profile</h1>
        <div class="profile-info">
            Welcome, <strong><%= user ? user.username : '' %></strong>!
        </div>
        <form action="/logout" method="get">
        <div style="display: flex; justify-content: center; gap: 0.7rem; margin-bottom: 1.2rem;">
            <form action="/logout" method="get" style="margin: 0;">
                <button type="submit" class="logout-btn">Log Out</button>
            </form>
            <a href="/edit-profile" class="logout-btn" style="text-align:center; text-decoration:none; line-height:2.2rem;">Edit Profile</a>
            <a href="/delete-profile" class="logout-btn" style="background: linear-gradient(90deg, #e74c3c 0%, #f7971e 100%); text-align:center; text-decoration:none; line-height:2.2rem;">Delete Profile</a>
        </div>
        <a href="/index.html" class="back-link">Back to Home</a>
    </div>
</body>
</html>
